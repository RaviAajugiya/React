function App() {
  const name ="Ravi"

  // {var} this not js it is evaluted expression(final evaluated js outcome)
  // we can't write if else in this
  return (
   <h1>React with Vite {name}</h1>
  )
}

export default App
